{
    "name": "next-multi-zones",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npm run dev --workspace=home & npm run dev --workspace=cart",
        "dev:home": "npm run dev --workspace=home",
        "dev:cart": "npm run dev --workspace=cart",
        "build": "npm run build --workspace=home && npm run build --workspace=cart",
        "start": "npm run start --workspace=home & npm run start --workspace=cart"
    },
    "workspaces": [
        "apps/*",
        "shared"
    ]
}